[build-system]
requires = ["maturin>=1.5"]
build-backend = "maturin"

[project]
name = "rusticol"
version = "0.1.0"
requires-python = ">=3.11"
description = "Rust/PyO3 runtime for pyAmpliCol eager-DAG process artifacts."

[tool.maturin]
features = ["pyo3/extension-module"]
