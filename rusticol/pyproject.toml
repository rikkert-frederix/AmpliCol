[build-system]
requires = ["maturin>=1.5"]
build-backend = "maturin"

[project]
name = "rusticol"
version = "0.1.0"
requires-python = ">=3.11"
description = "Python, C++, and Fortran runtime for pyAmpliCol schema-v2 process artifacts."

[tool.maturin]
features = ["extension-module"]
