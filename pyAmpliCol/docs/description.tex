\documentclass[10pt]{article}

\usepackage[margin=0.76in]{geometry}
\usepackage{amsmath,amssymb,mathtools}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{microtype}
\usepackage[table]{xcolor}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{pdflscape}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{array}

\hypersetup{
  colorlinks=true,
  linkcolor=blue!45!black,
  urlcolor=blue!45!black,
  citecolor=blue!45!black
}

\lstdefinestyle{pyamplicol}{
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\bfseries\color{blue!45!black},
  commentstyle=\itshape\color{black!55},
  stringstyle=\color{green!35!black},
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
  frame=single,
  framerule=0.25pt,
  xleftmargin=1.0em,
  xrightmargin=1.0em,
  aboveskip=0.55em,
  belowskip=0.55em
}
\lstset{style=pyamplicol}

\newcommand{\AC}{\textsc{AmpliCol}}
\newcommand{\PAC}{\textsc{pyAmpliCol}}
\newcommand{\J}{\mathcal{J}}
\newcommand{\V}{\mathcal{V}}
\newcommand{\M}{\mathcal{M}}
\newcommand{\heplib}{\texttt{hep\_lib}}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\definecolor{speedgreen}{HTML}{1A7F37}
\definecolor{speedorange}{HTML}{BF8700}
\definecolor{speedred}{HTML}{CF222E}
\definecolor{refblue}{HTML}{DCEBFF}
\definecolor{bestgreen}{HTML}{DFF5E4}
\definecolor{cdaggray}{HTML}{F4F5F7}

\title{\vspace{-2.0em}Current Recursion and Tensor-Network Execution in \PAC}
\author{}
\date{\vspace{-3.0em}}

\begin{document}
\maketitle
\vspace{-1.2em}

\paragraph{Purpose.}
This note summarizes the matrix-element generation strategy used by \AC\ and
the corresponding \PAC\ implementation.  The common idea is to avoid explicit
Feynman-diagram enumeration.  Instead, matrix elements are assembled from a
directed acyclic graph (DAG) of off-shell currents.  \AC\ emits Fortran code
that numerically sweeps this current table.  \PAC\ builds the same logical DAG
in Python, lowers bounded pieces to tensor-network expressions handled by
Spenso, compiles the resulting parametric scalar/vector expressions with
Symbolica evaluators, and can now execute the staged sweep from Rust through
the PyO3 runtime \texttt{rusticol}.

\section{The AmpliCol Current Recursion}

For a fixed leading-color ordering, a current is labelled by the external
subset it contains, the particle type carried by the off-shell line, and any
spin/chirality information.  Schematically, if $I$ is a set or ordered interval
of external colored legs, a current is computed as
\begin{equation}
  \J^\alpha_{t,\chi}(I)
  =
  P^\alpha{}_\beta(t, p_I)
  \sum_{\substack{I=A\cup B\\A\cap B=\emptyset}}
  \V^\beta{}_{\gamma\delta}(t_A,t_B\to t)\,
  \J^\gamma_{t_A,\chi_A}(A)\,
  \J^\delta_{t_B,\chi_B}(B).
  \label{eq:current-recursion}
\end{equation}
Here $t$ is the current type (gluon, quark, auxiliary tensor, etc.),
$\chi$ is the Weyl chirality where relevant, $p_I=\sum_{i\in I}p_i$, $P$ is
the propagator, and $\V$ is the appropriate Feynman-rule tensor.  The
four-gluon vertex is treated through the auxiliary antisymmetric tensor route,
so it is still represented by cubic current combinations.

\AC's optimized mode does not loop over full helicity configurations as
independent computations.  It weaves helicity information into the current
table itself.  Each external helicity state is a source current with a unique
bit in an \texttt{ext\_cur}-style bitset.  An internal current is identified by
\begin{equation}
  K(\J)=\bigl(t,\chi,\mathrm{bin}(I),\mathrm{ext\_cur}\bigr),
  \label{eq:current-key}
\end{equation}
where $\mathrm{bin}(I)$ records the external subset and $\mathrm{ext\_cur}$ is
the union of the source-current bits feeding the current.  When a new vertex
contribution produces the same key as an existing current, \AC\ appends that
vertex to the existing current rather than creating a duplicate node.  After
all currents are constructed, \texttt{curr2amp} records the two endpoint
currents whose contraction gives each retained helicity amplitude.

The generated Fortran library has the same layered structure:
\begin{lstlisting}[language=Fortran]
subroutine evaluate_amp(p, amps)
  complex(kind=8) :: val_c(1:6, n_cur)
  complex(kind=8) :: int_c(1:6, n_vert)

  call compute_external_currents(pp, val_c)
  do size = 2, n_external_minus_one
    call compute_vertices_size(pp, val_c, int_c)
    call compute_currents_size(pp, val_c, int_c)
  end do
  call compute_amps(amps, val_c)
end subroutine
\end{lstlisting}
The arrays \texttt{val\_c} and \texttt{int\_c} are the numerical current and
interaction tables.  This is the performance-critical idea that \PAC\ keeps:
one topological sweep produces all retained amplitudes, and shared parent
currents are evaluated once.

\section{The pyAmpliCol DAG}

\PAC\ first builds an explicit Python representation of the same current table.
For the leading-color milestone $q\bar q\to Z+n g$, the current identity is
\begin{equation}
  K_{\PAC}=\bigl(\mathrm{pdg},\mathrm{labels},\chi,\mathrm{source\_bits}\bigr).
\end{equation}
The \texttt{source\_bits} field is the direct analogue of \AC's
\texttt{ext\_cur}.  Source currents are created for the allowed quark,
antiquark, gluon, and massive-vector helicity states.  Internal currents are
then generated by increasing current size, with all interactions that feed the
same key attached to one node.

\begin{lstlisting}[language=Python]
for external helicity source s:
    add_current(key=(pdg_s, labels_s, chirality_s, bit_s), is_source=True)

for current_size in increasing_sizes:
    for compatible split A | B:
        result_key = combine_key(key[A], key[B], vertex_kind)
        result = add_or_reuse_current(result_key)
        add_interaction(left=A, right=B, result=result, vertex=vertex_kind)

for retained helicity ancestry:
    add_amplitude(left=current_with_Z_and_gluons, right=anti_current)

prune_dead_currents_after_helicity_filter()
\end{lstlisting}

At runtime, \PAC\ mirrors the Fortran table layout with contiguous arrays:
\[
  C_{s,i,c}\equiv\texttt{current\_rows}[s,i,c],
  \qquad
  T_{s,v,c}\equiv\texttt{interaction\_rows}[s,v,c],
\]
where $s$ is the phase-space sample in the current batch, $i$ is a current id,
$v$ is an interaction id, and $c$ is a component index.  External wavefunctions
fill source-current rows once per sample.  Bounded evaluator blocks then fill
interactions and derived currents stage by stage:
\begin{lstlisting}[language=Python]
fill_source_currents(current_rows, momenta, helicity_sources)

for stage in current_size_layers:
    state = ParamBuilder.pack(current_rows, interaction_rows, momenta)
    interaction_rows[:, stage.vertices] = vertex_eval[stage](state)
    current_rows[:, stage.currents]     = current_eval[stage](state)

amps = amplitude_eval(ParamBuilder.pack(current_rows, momenta))
me2  = normalization * sum(weight[h] * abs(amps[h])**2 for h in retained_h)
\end{lstlisting}
This is why the evaluator-only benchmark is meaningful: the expensive
recursion is inside compiled evaluator calls, while Python only performs source
wavefunction construction, parameter packing, and table transfers.

\section{Spenso Tensor Networks and \texttt{hep\_lib}}

Spenso is used to express Lorentz, spinor, auxiliary-tensor, and eventually
color contractions in a representation-aware way.  A tensor-network expression
contains named tensors with typed slots:
\[
  \V_{\mu\nu}^{\phantom{\mu\nu}\rho}(p_A,p_B)\,
  A^\mu\,B^\nu,
  \qquad
  \text{or in code: }
  \texttt{V(m("a"),m("b"),m("out")) * A(m("a")) * B(m("b"))}.
\]
The expression is not expanded.  It is a compact product of tensor factors with
shared representation labels.  The library \heplib\ is the dictionary that
tells Spenso what each tensor name means: dense model tensors, metric tensors,
momentum-dependent propagators, and parametric rank-one tensors representing
runtime inputs.

\begin{lstlisting}[language=Python]
library = model.build_tensor_library()      # returns TensorLibrary.hep_lib_atom()
builder = ParamBuilder()
mink = Representation.mink(4)

builder.register_rank1_tensor(
    library,
    tensor_name="current_A",
    representation=mink,
    head=("stage", "A"),
    length=4,
    role="block_current",
)
builder.register_rank1_tensor(
    library,
    tensor_name="p_A",
    representation=mink,
    head=("stage", "pA"),
    length=4,
    role="block_momentum",
    real_valued=True,
)

raw = V3g(mink("a"), mink("b"), mink("out"), p_A="p_A", p_B="p_B") \
    * TensorName("current_A")(mink("a")).to_expression() \
    * TensorName("current_B")(mink("b")).to_expression()

network = TensorNetwork(raw, library)
network.execute(library=library)
out_tensor = network.result_tensor(library)
evaluator = out_tensor.evaluator(params=builder.parameter_symbols())
\end{lstlisting}

The important point is that \heplib\ stays fixed during the execution.  New
graph currents are not registered as permanent physics tensors.  Instead, the
current values are runtime parameters for a bounded block.  Spenso executes the
factorized tensor network, contracts the representation indices, and returns a
parametric output tensor.  Symbolica then compiles that parametric tensor to an
evaluator.  For high multiplicity, \PAC\ interleaves this process: after a
current or current-size layer is contracted, its numerical output is written
back into the current table and becomes an input parameter for later blocks.
This avoids one giant scalar expression and keeps the DAG close to \AC's
execution model.

If explicit color tensors are introduced in future full-color modes, color
simplification belongs before Spenso execution:
\[
  \texttt{raw} \leftarrow \texttt{idenso.simplify\_color(raw)} ,
  \qquad
  \text{then build } \texttt{TensorNetwork(raw, hep\_lib)}.
\]
For the current leading-color milestone, color is already reduced to scalar
factors associated with the ordered amplitude.

\section{Example: \texorpdfstring{$d\bar d\to Zgg$}{d dbar -> Z g g}}

The user-level process is written as
\[
  d(p_1)\,\bar d(p_2)\to Z(p_Z)\,g(p_3)\,g(p_4).
\]
For the leading-color current recursion, the colored chain is treated in the
canonical order
\[
  \bar d \;-\; d \;-\; g_3 \;-\; g_4,
\]
with the color-singlet $Z$ inserted on the quark line.  The relevant source
currents are
\[
  \J_{\bar q}(1,h_1),\quad
  \J_q(2,h_2),\quad
  \epsilon^\mu(3,h_3),\quad
  \epsilon^\nu(4,h_4),\quad
  \epsilon_Z^\rho(Z,h_Z).
\]
The gluon subcurrents include
\begin{align}
  \J_g^\alpha(3,4;h_3,h_4)
   &= P_g{}^\alpha{}_\beta(p_3+p_4)
      \V_{3g}^{\beta\mu\nu}(p_3,p_4)
      \epsilon_\mu(3,h_3)\epsilon_\nu(4,h_4),\\
  \J_T^A(3,4;h_3,h_4)
   &= \V_{ggT}^{A\mu\nu}\epsilon_\mu(3,h_3)\epsilon_\nu(4,h_4),
\end{align}
where $\J_T$ is the auxiliary tensor current used to reproduce the four-gluon
structure in larger examples.  The quark line then forms
\begin{align}
  \J_q(2,3) &= P_q(p_2+p_3)\,\V_{qgq}\bigl(\J_q(2),\epsilon(3)\bigr),\\
  \J_q(2,3,4) &=
    P_q(p_2+p_3+p_4)\left[
      \V_{qgq}\bigl(\J_q(2),\J_g(3,4)\bigr)
      + \V_{qgq}\bigl(\J_q(2,3),\epsilon(4)\bigr)
    \right],
\end{align}
and the $Z$ can be inserted before, between, or after the ordered gluons:
\begin{align}
  \J_q(2,Z) &= P_q(p_2+p_Z)\,\V_{qZq}\bigl(\J_q(2),\epsilon_Z\bigr),\\
  \J_q(2,3,Z) &=
    P_q(p_2+p_3+p_Z)\left[
      \V_{qZq}\bigl(\J_q(2,3),\epsilon_Z\bigr)
      + \V_{qgq}\bigl(\J_q(2,Z),\epsilon(3)\bigr)
    \right],
\end{align}
with analogous terms leading to $\J_q(2,3,4,Z)$.  The helicity amplitude is
finally an endpoint contraction,
\begin{equation}
  \M(h_1,h_2,h_3,h_4,h_Z)
  =
  \J_q(2,3,4,Z;h_2,h_3,h_4,h_Z)
  \cdot
  \J_{\bar q}(1,h_1).
\end{equation}
Only retained helicity ancestries survive the filter.  The reported squared
matrix element has the same structure as in \AC:
\begin{equation}
  |\M|^2_{\mathrm{LC}}
  =
  \mathcal{N}_{\mathrm{AC}}
  \sum_{h\in H_{\mathrm{kept}}}
  w_h\,
  \left|\M_h\right|^2,
\end{equation}
where $\mathcal{N}_{\mathrm{AC}}$ contains the leading-color normalization,
couplings, symmetry factors, and initial-state averaging conventions matched
to the Fortran implementation.


\section{Rusticol: the Implemented Rust Runtime}

The previous staged \PAC\ runtime proved that the shared-current DAG could
match Fortran \AC\ numerically, but its Python loop still moved current tables
between many evaluator calls.  The current implementation keeps Python as the
process generator and validation driver, but moves the hot eager-DAG sweep into
a PyO3 extension named \texttt{rusticol}.  The generated artifact is now a
self-contained process directory used by both runtimes:
\begin{lstlisting}[language=Python]
rt_py = pyamplicol.load_process(process_dir, runtime="python")
values_py = rt_py.evaluate(momenta)

import rusticol
rt = rusticol.Runtime.load(process_dir)
values = rt.evaluate(momenta)
profile = rt.profile(momenta)
\end{lstlisting}
The Python and Rust runtimes read the same \texttt{process\_manifest.json}, the
same Symbolica evaluator manifests, and the same validation momenta.  The
Python path is kept as a transparent reference implementation.  The Rust path is
the performance path.

A process directory contains four classes of data.  First, it records process
and model metadata: external PDG order, momentum conventions, particle and
vertex tables, couplings, masses, symmetry factors, and leading-color
normalization.  Second, it stores the shared-current DAG: source currents,
current ids, dimensions, interaction stages, output slots, retained amplitude
records, helicity weights, and color factors.  Third, it stores the compiled
Symbolica evaluator artifacts for each stage and for the final amplitude/raw-sum
stage.  Fourth, it contains standalone validation assets:
\texttt{validation\_momenta.json} with decimal-string momenta and a
\texttt{check\_standalone.py} script that imports only \texttt{rusticol}, plus
\texttt{numpy} when available for the optimized f64 input path.

\begin{lstlisting}[language=Python]
# Generated by pyAmpliCol.
process_dir/
  process_manifest.json
  manifest.json
  validation_momenta.json
  check_standalone.py
  compiled/              # C++/ASM shared libraries when present
  ... evaluator states ...
\end{lstlisting}

\texttt{rusticol.Runtime.load} validates the schema, checks that the current
process family is supported, loads every Symbolica evaluator, computes direct
state offsets for stage outputs, allocates reusable scratch buffers, and builds
a fixed execution plan.  The optimized f64 path accepts NumPy arrays with shape
\texttt{(batch, n\_external, 4)}.  For each batch, Rust fills source
wavefunctions and momentum sums, executes stage evaluators in topological order,
writes the returned current components into the contiguous state table, executes
the amplitude/raw-sum evaluator, applies the Fortran-matched normalization, and
returns a NumPy array of matrix elements.
\begin{lstlisting}
for point in batch {
    fill_source_currents(state_row, point);
    fill_momentum_sums(state_row, point);
}

for stage in stages {
    evaluated = stage.evaluator.evaluate_batch(state);
    assign_stage_outputs(state, evaluated, stage.output_offsets);
}

raw = amplitude_stage.evaluate_raw_sums(state);
me2 = normalization * raw;
\end{lstlisting}
The \texttt{profile} API reports the same split used in the benchmarks: source
fill, momentum setup, stage evaluator time, output assignment, amplitude
evaluator time, final reduction, total wall time, and memory snapshots where the
platform exposes them.

The precision API follows Symbolica's model.  \texttt{evaluate} and
\texttt{evaluate\_with\_prec(..., 16)} use the specialized native f64 path.
\texttt{evaluate\_with\_prec(..., 32)} maps the serialized exact evaluator state
to Symbolica's \texttt{DoubleFloat} coefficients.  Other positive precisions map
to arbitrary-precision \texttt{Float} evaluators.  The high-precision routes
share the same stage orchestration code through generic Rust traits, while the
f64 route remains specialized and avoids high-precision allocation overhead.

The artifact schema remains process-generic.  The implemented fast family is
leading-color \(q\bar q\to Z+n g\); \(W^+\), \(e^+e^-\), and \(ZZ\) probes
currently raise explicit unsupported-process diagnostics.


\begin{landscape}
\section{Performance Results}

Tables~\ref{tab:combined-results} and the markdown files
\texttt{performance\_summary.md} and
\texttt{performance\_summary\_compiled\_dag.md} report the current benchmark
state for \(d\bar d\to Z+n g\), \(n=1,\ldots,9\).  All timing values are shown
in monospace font.  Green, orange, and red timing text follow the markdown
convention: faster than Fortran \AC, slower but below a factor two, and slower
by a factor two or more.  The blue rows are the Fortran \AC\ references.  The
green highlighted rows are the present best production route, \texttt{rusticol}
with C++ O3 stage evaluators.

\scriptsize
\setlength{\tabcolsep}{2.1pt}
\renewcommand{\arraystretch}{1.13}
\begin{longtable}{@{}r L{0.95in} L{1.72in} r r r L{3.07in}@{}}
\caption{Combined performance summary for staged/Rusticol and compiled-DAG routes.}\label{tab:combined-results}\\
\toprule
\textbf{n} & \textbf{route} & \textbf{setup} & \textbf{gen [s]} & \textbf{wall [us/pt]} & \textbf{eval [us/pt]} & \textbf{notes} \\
\midrule
\endfirsthead
\toprule
\textbf{n} & \textbf{route} & \textbf{setup} & \textbf{gen [s]} & \textbf{wall [us/pt]} & \textbf{eval [us/pt]} & \textbf{notes} \\
\midrule
\endhead
\midrule
\multicolumn{7}{r}{Continued on next page}\\
\endfoot
\bottomrule
\endlastfoot
\rowcolor{refblue} \textbf{1} & \textbf{reference} & \textbf{AmpliCol} & \textbf{\texttt{2.22}} & \textbf{\texttt{N/A}} & \textbf{\texttt{0.734}} & Fortran AmpliCol \\
1 & staged/Rusticol & pyAmpliCol - JIT & \textcolor{speedgreen}{\texttt{0.016 (x0.01)}} & \textcolor{speedred}{\texttt{13.34(29) (x18.17)}} & \textcolor{speedgreen}{\texttt{0.607(17) (x0.83)}} & JIT; batch=16 \\
1 & staged/Rusticol & pyAmpliCol - C++ & \textcolor{speedgreen}{\texttt{0.774 (x0.35)}} & \textcolor{speedred}{\texttt{13.01(12) (x17.72)}} & \textcolor{speedgreen}{\texttt{0.521(10) (x0.71)}} & C++; O3; chunk=None; batch=16 \\
1 & staged/Rusticol & pyAmpliCol - ASM & \textcolor{speedgreen}{\texttt{0.560 (x0.25)}} & \textcolor{speedred}{\texttt{14.03(30) (x19.12)}} & \textcolor{speedorange}{\texttt{0.912(18) (x1.24)}} & ASM; O3; chunk=None; batch=16 \\
\rowcolor{bestgreen} 1 & staged/Rusticol & \textbf{pyAmpliCol - Rusticol} & \textcolor{speedgreen}{\texttt{0.984 (x0.44)}} & \textcolor{speedgreen}{\texttt{0.412(1) (x0.56)}} & \textcolor{speedgreen}{\texttt{0.1938(2) (x0.26)}} & Rusticol PyO3; C++ O3 process artifact; cached NumPy input; reusable stage scratch; single-chunk direct output; batch=1024 \\
\rowcolor{cdaggray} 1 & compiled-DAG & pyAmpliCol - compiled-DAG JIT & \textcolor{speedgreen}{\texttt{0.0483 (x0.02)}} & \textcolor{speedgreen}{\texttt{0.660 (x0.90)}} & \textcolor{speedgreen}{\texttt{0.108 (x0.15)}} & JIT; source and propagator-denominator aliases; batch 256; full-batch warm-up \\
\rowcolor{cdaggray} 1 & compiled-DAG & pyAmpliCol - compiled-DAG C++ & \textcolor{speedgreen}{\texttt{0.560 (x0.25)}} & \textcolor{speedorange}{\texttt{0.768 (x1.05)}} & \textcolor{speedgreen}{\texttt{0.189 (x0.26)}} & C++; O3; chunk=None; batch 256; saved evaluator \\
\rowcolor{cdaggray} 1 & compiled-DAG & pyAmpliCol - compiled-DAG ASM & \textcolor{speedgreen}{\texttt{0.297 (x0.13)}} & \textcolor{speedorange}{\texttt{0.978 (x1.33)}} & \textcolor{speedgreen}{\texttt{0.403 (x0.55)}} & ASM; O3; chunk=None; batch 256; saved evaluator \\
\midrule[0.58pt]
\rowcolor{refblue} \textbf{2} & \textbf{reference} & \textbf{AmpliCol} & \textbf{\texttt{2.20}} & \textbf{\texttt{N/A}} & \textbf{\texttt{1.64}} & Fortran AmpliCol \\
2 & staged/Rusticol & pyAmpliCol - JIT & \textcolor{speedgreen}{\texttt{0.066 (x0.03)}} & \textcolor{speedred}{\texttt{16.15(15) (x9.85)}} & \textcolor{speedgreen}{\texttt{1.61(2) (x0.98)}} & JIT; batch=16 \\
2 & staged/Rusticol & pyAmpliCol - C++ & \textcolor{speedgreen}{\texttt{1.86 (x0.85)}} & \textcolor{speedred}{\texttt{16.05(14) (x9.79)}} & \textcolor{speedgreen}{\texttt{1.16(2) (x0.70)}} & C++; O3; chunk=None; batch=16 \\
2 & staged/Rusticol & pyAmpliCol - ASM & \textcolor{speedgreen}{\texttt{0.940 (x0.43)}} & \textcolor{speedred}{\texttt{16.48(20) (x10.05)}} & \textcolor{speedorange}{\texttt{2.16(2) (x1.32)}} & ASM; O3; chunk=None; batch=16 \\
\rowcolor{bestgreen} 2 & staged/Rusticol & \textbf{pyAmpliCol - Rusticol} & \textcolor{speedgreen}{\texttt{2.00 (x0.91)}} & \textcolor{speedgreen}{\texttt{0.984(1) (x0.60)}} & \textcolor{speedgreen}{\texttt{0.653(1) (x0.40)}} & Rusticol PyO3; C++ O3 process artifact; cached NumPy input; reusable stage scratch; single-chunk direct output; batch=1024 \\
\rowcolor{cdaggray} 2 & compiled-DAG & pyAmpliCol - compiled-DAG JIT & \textcolor{speedgreen}{\texttt{0.0941 (x0.04)}} & \textcolor{speedgreen}{\texttt{1.10 (x0.67)}} & \textcolor{speedgreen}{\texttt{0.421 (x0.26)}} & JIT; source and propagator-denominator aliases; batch 256; full-batch warm-up \\
\rowcolor{cdaggray} 2 & compiled-DAG & pyAmpliCol - compiled-DAG C++ & \textcolor{speedgreen}{\texttt{0.726 (x0.33)}} & \textcolor{speedgreen}{\texttt{1.22 (x0.74)}} & \textcolor{speedgreen}{\texttt{0.512 (x0.31)}} & C++; O3; chunk=None; batch 256; saved evaluator \\
\rowcolor{cdaggray} 2 & compiled-DAG & pyAmpliCol - compiled-DAG ASM & \textcolor{speedgreen}{\texttt{0.379 (x0.17)}} & \textcolor{speedorange}{\texttt{2.24 (x1.36)}} & \textcolor{speedgreen}{\texttt{1.51 (x0.92)}} & ASM; O3; chunk=None; batch 256; saved evaluator \\
\midrule[0.58pt]
\rowcolor{refblue} \textbf{3} & \textbf{reference} & \textbf{AmpliCol} & \textbf{\texttt{2.31}} & \textbf{\texttt{N/A}} & \textbf{\texttt{4.55}} & Fortran AmpliCol \\
3 & staged/Rusticol & pyAmpliCol - JIT & \textcolor{speedgreen}{\texttt{0.281 (x0.12)}} & \textcolor{speedred}{\texttt{22.67(42) (x4.98)}} & \textcolor{speedorange}{\texttt{4.72(5) (x1.04)}} & JIT; batch=16 \\
3 & staged/Rusticol & pyAmpliCol - C++ & \textcolor{speedred}{\texttt{5.69 (x2.46)}} & \textcolor{speedred}{\texttt{20.90(40) (x4.59)}} & \textcolor{speedgreen}{\texttt{3.06(4) (x0.67)}} & C++; O3; chunk=None; batch=16 \\
3 & staged/Rusticol & pyAmpliCol - ASM & \textcolor{speedgreen}{\texttt{1.52 (x0.66)}} & \textcolor{speedred}{\texttt{23.87(45) (x5.25)}} & \textcolor{speedorange}{\texttt{6.17(8) (x1.36)}} & ASM; O3; chunk=None; batch=16 \\
\rowcolor{bestgreen} 3 & staged/Rusticol & \textbf{pyAmpliCol - Rusticol} & \textcolor{speedred}{\texttt{5.48 (x2.37)}} & \textcolor{speedgreen}{\texttt{2.783(13) (x0.61)}} & \textcolor{speedgreen}{\texttt{2.177(10) (x0.48)}} & Rusticol PyO3; C++ O3 process artifact; cached NumPy input; reusable stage scratch; batch=128 \\
\rowcolor{cdaggray} 3 & compiled-DAG & pyAmpliCol - compiled-DAG JIT & \textcolor{speedgreen}{\texttt{0.313 (x0.14)}} & \textcolor{speedgreen}{\texttt{3.18 (x0.70)}} & \textcolor{speedgreen}{\texttt{2.35 (x0.52)}} & JIT; source and propagator-denominator aliases; batch 256; full-batch warm-up \\
\rowcolor{cdaggray} 3 & compiled-DAG & pyAmpliCol - compiled-DAG C++ & \textcolor{speedorange}{\texttt{2.84 (x1.23)}} & \textcolor{speedgreen}{\texttt{2.54 (x0.56)}} & \textcolor{speedgreen}{\texttt{1.70 (x0.37)}} & C++; O3; chunk=None; batch 256; saved evaluator \\
\rowcolor{cdaggray} 3 & compiled-DAG & pyAmpliCol - compiled-DAG ASM & \textcolor{speedgreen}{\texttt{0.715 (x0.31)}} & \textcolor{speedorange}{\texttt{5.51 (x1.21)}} & \textcolor{speedorange}{\texttt{4.65 (x1.02)}} & ASM; O3; chunk=None; batch 256; saved evaluator \\
\midrule[0.58pt]
\rowcolor{refblue} \textbf{4} & \textbf{reference} & \textbf{AmpliCol} & \textbf{\texttt{2.55}} & \textbf{\texttt{N/A}} & \textbf{\texttt{13.2}} & Fortran AmpliCol \\
4 & staged/Rusticol & pyAmpliCol - JIT & \textcolor{speedgreen}{\texttt{0.252 (x0.10)}} & \textcolor{speedred}{\texttt{49.53(42) (x3.75)}} & \textcolor{speedred}{\texttt{28.37(17) (x2.15)}} & JIT; batch=16 \\
4 & staged/Rusticol & pyAmpliCol - C++ & \textcolor{speedred}{\texttt{20.7 (x8.14)}} & \textcolor{speedred}{\texttt{27.78(13) (x2.10)}} & \textcolor{speedgreen}{\texttt{7.75(6) (x0.59)}} & C++; O3; chunk=None; batch=16 \\
4 & staged/Rusticol & pyAmpliCol - ASM & \textcolor{speedgreen}{\texttt{2.04 (x0.80)}} & \textcolor{speedred}{\texttt{36.30(54) (x2.75)}} & \textcolor{speedorange}{\texttt{15.80(15) (x1.20)}} & ASM; O3; chunk=None; batch=16 \\
\rowcolor{bestgreen} 4 & staged/Rusticol & \textbf{pyAmpliCol - Rusticol} & \textcolor{speedred}{\texttt{19.9 (x7.81)}} & \textcolor{speedgreen}{\texttt{7.23(7) (x0.55)}} & \textcolor{speedgreen}{\texttt{6.10(6) (x0.46)}} & Rusticol PyO3; C++ O3 process artifact; cached NumPy input; reusable stage scratch; batch=128 \\
\rowcolor{cdaggray} 4 & compiled-DAG & pyAmpliCol - compiled-DAG JIT & \textcolor{speedgreen}{\texttt{0.301 (x0.12)}} & \textcolor{speedgreen}{\texttt{11.8 (x0.89)}} & \textcolor{speedgreen}{\texttt{10.9 (x0.82)}} & JIT; source and propagator-denominator aliases; batch 256; full-batch warm-up \\
\rowcolor{cdaggray} 4 & compiled-DAG & pyAmpliCol - compiled-DAG C++ & \textcolor{speedred}{\texttt{40.6 (x15.9)}} & \textcolor{speedorange}{\texttt{16.8 (x1.27)}} & \textcolor{speedorange}{\texttt{15.7 (x1.19)}} & C++; O3; chunk=None; batch 256; saved evaluator; monolithic C++ slower than JIT \\
\rowcolor{cdaggray} 4 & compiled-DAG & pyAmpliCol - compiled-DAG ASM & \textcolor{speedgreen}{\texttt{1.02 (x0.40)}} & \textcolor{speedorange}{\texttt{18.5 (x1.40)}} & \textcolor{speedorange}{\texttt{17.4 (x1.32)}} & ASM; O3; chunk=None; batch 256; saved evaluator \\
\midrule[0.58pt]
\rowcolor{refblue} \textbf{5} & \textbf{reference} & \textbf{AmpliCol} & \textbf{\texttt{3.22}} & \textbf{\texttt{N/A}} & \textbf{\texttt{34.3}} & Fortran AmpliCol \\
5 & staged/Rusticol & pyAmpliCol - JIT & \textcolor{speedgreen}{\texttt{0.327 (x0.10)}} & \textcolor{speedred}{\texttt{373.6(2.8) (x10.89)}} & \textcolor{speedred}{\texttt{342.7(2.6) (x9.99)}} & JIT; batch=16 \\
5 & staged/Rusticol & pyAmpliCol - C++ & \textcolor{speedred}{\texttt{54.3 (x16.87)}} & \textcolor{speedorange}{\texttt{45.06(48) (x1.31)}} & \textcolor{speedgreen}{\texttt{20.83(26) (x0.61)}} & C++; O3; chunk=64; batch=128; chunk compile workers=10; two-stage save/load \\
5 & staged/Rusticol & pyAmpliCol - C++ (O2) & \textcolor{speedred}{\texttt{50.7 (x15.76)}} & \textcolor{speedorange}{\texttt{51.04(87) (x1.49)}} & \textcolor{speedgreen}{\texttt{23.82(42) (x0.69)}} & C++; O2; chunk=64; batch=16 \\
5 & staged/Rusticol & pyAmpliCol - ASM & \textcolor{speedred}{\texttt{10.2 (x3.16)}} & \textcolor{speedred}{\texttt{75.69(88) (x2.21)}} & \textcolor{speedorange}{\texttt{48.74(72) (x1.42)}} & ASM; O3; chunk=64; batch=16 \\
\rowcolor{bestgreen} 5 & staged/Rusticol & \textbf{pyAmpliCol - Rusticol} & \textcolor{speedred}{\texttt{50.2 (x15.60)}} & \textcolor{speedgreen}{\texttt{19.63(28) (x0.57)}} & \textcolor{speedgreen}{\texttt{17.50(25) (x0.51)}} & Rusticol PyO3; C++ O3; chunk=64; cached NumPy input; reusable stage scratch; batch=128; two-stage save/load \\
\rowcolor{cdaggray} 5 & compiled-DAG & pyAmpliCol - compiled-DAG JIT & \textcolor{speedgreen}{\texttt{0.435 (x0.14)}} & \textcolor{speedgreen}{\texttt{32.8 (x0.96)}} & \textcolor{speedgreen}{\texttt{31.0 (x0.90)}} & JIT; source and propagator-denominator aliases; batch 32; full-batch warm-up \\
\rowcolor{cdaggray} 5 & compiled-DAG & pyAmpliCol - compiled-DAG C++ & \texttt{N/A} & \texttt{N/A} & \texttt{N/A} & C++; O3; chunk=None; monolithic compile did not complete productively \\
\rowcolor{cdaggray} 5 & compiled-DAG & pyAmpliCol - compiled-DAG ASM & \textcolor{speedgreen}{\texttt{1.94 (x0.60)}} & \textcolor{speedorange}{\texttt{49.5 (x1.44)}} & \textcolor{speedorange}{\texttt{47.6 (x1.39)}} & ASM; O3; chunk=None; batch 32; saved evaluator \\
\midrule[0.58pt]
\rowcolor{refblue} \textbf{6} & \textbf{reference} & \textbf{AmpliCol} & \textbf{\texttt{4.55}} & \textbf{\texttt{N/A}} & \textbf{\texttt{84.1}} & Fortran AmpliCol \\
6 & staged/Rusticol & pyAmpliCol - JIT & \textcolor{speedgreen}{\texttt{1.02 (x0.22)}} & \textcolor{speedred}{\texttt{1435(10) (x17.06)}} & \textcolor{speedred}{\texttt{1380(8) (x16.41)}} & JIT; batch=16 \\
6 & staged/Rusticol & pyAmpliCol - C++ & \textcolor{speedred}{\texttt{151 (x33.23)}} & \textcolor{speedorange}{\texttt{94.79(73) (x1.13)}} & \textcolor{speedgreen}{\texttt{56.36(49) (x0.67)}} & C++; O3; chunk=64; batch=128; chunk compile workers=10; two-stage save/load \\
6 & staged/Rusticol & pyAmpliCol - C++ (O2) & \textcolor{speedred}{\texttt{140 (x30.74)}} & \textcolor{speedorange}{\texttt{108.4(1.8) (x1.29)}} & \textcolor{speedgreen}{\texttt{68.5(1.6) (x0.81)}} & C++; O2; chunk=64; batch=16 \\
6 & staged/Rusticol & pyAmpliCol - ASM & \textcolor{speedred}{\texttt{21.4 (x4.70)}} & \textcolor{speedred}{\texttt{183.7(3.3) (x2.18)}} & \textcolor{speedorange}{\texttt{141.2(2.7) (x1.68)}} & ASM; O3; chunk=64; batch=16 \\
\rowcolor{bestgreen} 6 & staged/Rusticol & \textbf{pyAmpliCol - Rusticol} & \textcolor{speedred}{\texttt{141 (x31.08)}} & \textcolor{speedgreen}{\texttt{55.63(26) (x0.66)}} & \textcolor{speedgreen}{\texttt{50.12(23) (x0.60)}} & Rusticol PyO3; C++ O3; chunk=64; cached NumPy input; reusable stage scratch; batch=128; two-stage save/load \\
\rowcolor{cdaggray} 6 & compiled-DAG & pyAmpliCol - compiled-DAG JIT & \textcolor{speedgreen}{\texttt{1.26 (x0.28)}} & \textcolor{speedorange}{\texttt{89.7 (x1.07)}} & \textcolor{speedorange}{\texttt{87.7 (x1.04)}} & JIT; adaptive CPE unbounded; source and propagator-denominator aliases; batch 32; full-batch warm-up; max rel diff 5.1e-14 over 5 probe points \\
\rowcolor{cdaggray} 6 & compiled-DAG & pyAmpliCol - compiled-DAG C++ & \textcolor{speedred}{\texttt{814 (x179)}} & \textcolor{speedorange}{\texttt{108 (x1.28)}} & \textcolor{speedorange}{\texttt{106 (x1.26)}} & C++; O3; chunk=None; older completed probe; generation is not practical \\
\rowcolor{cdaggray} 6 & compiled-DAG & pyAmpliCol - compiled-DAG ASM & \textcolor{speedgreen}{\texttt{3.99 (x0.88)}} & \textcolor{speedorange}{\texttt{121 (x1.44)}} & \textcolor{speedorange}{\texttt{120 (x1.42)}} & ASM; O3; chunk=None; batch 1024 \\
\midrule[0.58pt]
\rowcolor{refblue} \textbf{7} & \textbf{reference} & \textbf{AmpliCol} & \textbf{\texttt{9.38}} & \textbf{\texttt{N/A}} & \textbf{\texttt{207}} & Fortran AmpliCol \\
7 & staged/Rusticol & pyAmpliCol - JIT & \texttt{N/A} & \texttt{N/A} & \texttt{N/A} & JIT backend bugged. \\
7 & staged/Rusticol & pyAmpliCol - C++ & \textcolor{speedred}{\texttt{417 (x44.44)}} & \textcolor{speedorange}{\texttt{259.7(1.4) (x1.25)}} & \textcolor{speedgreen}{\texttt{194.7(1.2) (x0.94)}} & C++; O3; chunk=96; batch=128; chunk compile workers=10; two-stage save/load \\
7 & staged/Rusticol & pyAmpliCol - C++ (O1) & \textcolor{speedred}{\texttt{219 (x23.35)}} & \textcolor{speedred}{\texttt{421.6(2.4) (x2.04)}} & \textcolor{speedorange}{\texttt{348.9(1.9) (x1.69)}} & C++; O1; chunk=96; batch=16 \\
7 & staged/Rusticol & pyAmpliCol - ASM & \textcolor{speedred}{\texttt{36.2 (x3.86)}} & \textcolor{speedorange}{\texttt{384.4(2.8) (x1.86)}} & \textcolor{speedorange}{\texttt{320.2(2.4) (x1.55)}} & ASM; O3; chunk=96; batch=16 \\
\rowcolor{bestgreen} 7 & staged/Rusticol & \textbf{pyAmpliCol - Rusticol} & \textcolor{speedred}{\texttt{391 (x41.71)}} & \textcolor{speedgreen}{\texttt{197.8(4) (x0.96)}} & \textcolor{speedgreen}{\texttt{186.1(4) (x0.90)}} & Rusticol PyO3; C++ O3; chunk=96; cached NumPy input; reusable stage scratch; batch=128; two-stage save/load \\
\rowcolor{cdaggray} 7 & compiled-DAG & pyAmpliCol - compiled-DAG JIT & \textcolor{speedgreen}{\texttt{3.90 (x0.42)}} & \textcolor{speedorange}{\texttt{268 (x1.29)}} & \textcolor{speedorange}{\texttt{265 (x1.28)}} & JIT; adaptive CPE unbounded; source and propagator-denominator aliases; batch 24; full-batch warm-up; max rel diff 3.7e-13 over 3 probe points \\
\rowcolor{cdaggray} 7 & compiled-DAG & pyAmpliCol - compiled-DAG C++ & \texttt{N/A} & \texttt{N/A} & \texttt{N/A} & C++; O3; chunk=None; not attempted after monolithic n=5 C++ compile failed productively \\
\rowcolor{cdaggray} 7 & compiled-DAG & pyAmpliCol - compiled-DAG ASM & \textcolor{speedorange}{\texttt{11.1 (x1.19)}} & \textcolor{speedorange}{\texttt{369 (x1.78)}} & \textcolor{speedorange}{\texttt{364 (x1.76)}} & ASM; O3; chunk=None; batch 512 \\
\midrule[0.58pt]
\rowcolor{refblue} \textbf{8} & \textbf{reference} & \textbf{AmpliCol} & \textbf{\texttt{25.0}} & \textbf{\texttt{N/A}} & \textbf{\texttt{567}} & Fortran AmpliCol \\
8 & staged/Rusticol & pyAmpliCol - JIT & \texttt{N/A} & \texttt{N/A} & \texttt{N/A} & JIT backend bugged. \\
8 & staged/Rusticol & pyAmpliCol - C++ & \textcolor{speedred}{\texttt{1.06e3 (x42.24)}} & \textcolor{speedorange}{\texttt{642.6(2.1) (x1.13)}} & \textcolor{speedgreen}{\texttt{491.8(1.4) (x0.87)}} & C++; O3; chunk=96; batch=128; chunk compile workers=10; two-stage save/load \\
8 & staged/Rusticol & pyAmpliCol - C++ (O1) & \textcolor{speedred}{\texttt{577 (x23.09)}} & \textcolor{speedorange}{\texttt{997.2(3.3) (x1.76)}} & \textcolor{speedorange}{\texttt{885.3(2.9) (x1.56)}} & C++; O1; chunk=96; batch=16 \\
8 & staged/Rusticol & pyAmpliCol - ASM & \textcolor{speedred}{\texttt{85.6 (x3.42)}} & \textcolor{speedorange}{\texttt{1117.0(4.6) (x1.97)}} & \textcolor{speedorange}{\texttt{1004.9(4.3) (x1.77)}} & ASM; O3; chunk=96; batch=16 \\
\rowcolor{bestgreen} 8 & staged/Rusticol & \textbf{pyAmpliCol - Rusticol} & \textcolor{speedred}{\texttt{1.02e3 (x40.90)}} & \textcolor{speedgreen}{\texttt{487.7(9) (x0.86)}} & \textcolor{speedgreen}{\texttt{464.4(6) (x0.82)}} & Rusticol PyO3; C++ O3; chunk=96; cached NumPy input; reusable stage scratch; batch=128; two-stage save/load \\
\rowcolor{cdaggray} 8 & compiled-DAG & pyAmpliCol - compiled-DAG JIT & \textcolor{speedgreen}{\texttt{16.6 (x0.67)}} & \textcolor{speedred}{\texttt{3601 (x6.35)}} & \textcolor{speedred}{\texttt{3596 (x6.34)}} & JIT; source and propagator-denominator aliases; batch 512 \\
\rowcolor{cdaggray} 8 & compiled-DAG & pyAmpliCol - compiled-DAG C++ & \texttt{N/A} & \texttt{N/A} & \texttt{N/A} & C++; O3; chunk=None; not attempted after monolithic n=5 C++ compile failed productively \\
\rowcolor{cdaggray} 8 & compiled-DAG & pyAmpliCol - compiled-DAG ASM & \textcolor{speedorange}{\texttt{34.7 (x1.39)}} & \textcolor{speedred}{\texttt{1432 (x2.53)}} & \textcolor{speedred}{\texttt{1430 (x2.52)}} & ASM O3; inline external wavefunctions; max rel diff 1.4e-13 over 3 probe points \\
\midrule[0.58pt]
\rowcolor{refblue} \textbf{9} & \textbf{reference} & \textbf{AmpliCol} & \textbf{\texttt{48.0}} & \textbf{\texttt{N/A}} & \textbf{\texttt{2520}} & Fortran AmpliCol \\
9 & staged/Rusticol & pyAmpliCol - JIT & \texttt{N/A} & \texttt{N/A} & \texttt{N/A} & JIT backend bugged. \\
9 & staged/Rusticol & pyAmpliCol - C++ & \textcolor{speedred}{\texttt{2.83e3 (x58.83)}} & \textcolor{speedgreen}{\texttt{1504.9(11.7) (x0.60)}} & \textcolor{speedgreen}{\texttt{1252.7(9.6) (x0.50)}} & C++; O3; chunk=96; batch=128; chunk compile workers=10; two-stage save/load \\
9 & staged/Rusticol & pyAmpliCol - ASM & \textcolor{speedred}{\texttt{349 (x7.27)}} & \textcolor{speedorange}{\texttt{2688.2(5.5) (x1.07)}} & \textcolor{speedgreen}{\texttt{2459.1(4.5) (x0.98)}} & ASM; O3; chunk=64; batch=16 \\
\rowcolor{bestgreen} 9 & staged/Rusticol & \textbf{pyAmpliCol - Rusticol} & \textcolor{speedred}{\texttt{2.83e3 (x58.83)}} & \textcolor{speedgreen}{\texttt{1164.8(9) (x0.46)}} & \textcolor{speedgreen}{\texttt{1120.9(1.0) (x0.44)}} & Rusticol PyO3; C++ O3; chunk=96; cached NumPy input; reusable stage scratch; batch=128; repackaged from existing C++ O3 artifact in 103 s; two-stage save/load \\
\rowcolor{cdaggray} 9 & compiled-DAG & pyAmpliCol - compiled-DAG JIT & \textcolor{speedred}{\texttt{122 (x2.54)}} & \textcolor{speedred}{\texttt{8056 (x3.20)}} & \textcolor{speedred}{\texttt{8051 (x3.19)}} & JIT; inline external wavefunctions; fresh Fortran probe currently fails with reference-side SIGSEGV \\
\rowcolor{cdaggray} 9 & compiled-DAG & pyAmpliCol - compiled-DAG C++ & \texttt{N/A} & \texttt{N/A} & \texttt{N/A} & C++; O3; chunk=None; not attempted after monolithic n=5 C++ compile failed productively \\
\rowcolor{cdaggray} 9 & compiled-DAG & pyAmpliCol - compiled-DAG ASM & \textcolor{speedred}{\texttt{191 (x3.97)}} & \textcolor{speedred}{\texttt{10705 (x4.25)}} & \textcolor{speedred}{\texttt{10700 (x4.25)}} & ASM; O3; chunk=None; batch 512; peak RSS 8.01 GiB \\
\end{longtable}
\end{landscape}


\section{Why This Matches AmpliCol but Remains Python-Friendly}

The Python layer owns graph construction, process metadata, deterministic
phase-space steering, artifact generation, evaluator caching, and validation
against \AC.  It does not perform the hot current recursion component by
component in production.  The two production execution routes are now:
\begin{align*}
  \text{Python staged route:}&\quad
  \text{source fill} \to \text{bounded stage evaluators} \to
  \text{amplitude/raw-sum evaluator},\\
  \text{Rusticol route:}&\quad
  \text{one PyO3 call} \to \text{Rust source fill and staged sweep} \to
  \text{amplitude/raw-sum evaluator}.
\end{align*}
Thus \PAC\ keeps \AC's essential optimization - shared currents across helicity
parents and a topological current-table sweep - while replacing generated
Fortran subroutines by saved Spenso/Symbolica evaluator artifacts.  The
\texttt{rusticol} path removes the remaining Python orchestration cost by
loading the same process directory and executing the sweep in Rust.

The fully monolithic compiled-DAG route is still valuable as a research backend:
it inlines the current graph into a single multi-output evaluator and reaches
excellent low-multiplicity performance.  At larger multiplicity, however, its
code shape no longer resembles the staged current-table schedule closely enough.
The best current production compromise is therefore Python generation plus a
Rust eager-DAG runtime over saved Symbolica stage evaluators.

\end{document}
