[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyamplicol"
version = "0.0.1"
description = "Python interface and tooling foundation for AmpliCol."
readme = "README.md"
requires-python = ">=3.11"
authors = [
    { name = "AmpliCol developers" },
]
dependencies = [
    "progressbar2",
    "colorama",
    "tabled",
    "numpy<2.5",
    "ufo_model_loader @ git+https://github.com/alphal00p/ufo_model_loader.git@9cb4deeae40ddd64184049af07ac1d03ce5f6162",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "mypy",
    "ipykernel",
]

[project.scripts]
pyamplicol = "pyamplicol.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
pyamplicol = [
    "py.typed",
    "api_templates/**/*",
    "assets/models/**/*.py",
    "assets/models/**/*.dat",
    "assets/models/**/*.json",
    "assets/models/MANIFEST.sha256",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-ra"
markers = [
    "dependencies: dependency installer and local dependency import tests",
    "integration: integration tests grouped by feature area",
    "slow: tests that are intentionally slower and should be batched before full-suite gates",
    "typing: static typing and mypy gate tests",
    "unit: focused unit tests grouped by feature area",
]

[tool.mypy]
python_version = "3.11"
mypy_path = "src"
files = ["src/pyamplicol", "tests"]
show_error_codes = true
pretty = true
warn_unused_ignores = true
